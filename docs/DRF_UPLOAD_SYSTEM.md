# Digital RF Upload System

Digital RF outputs and PSWS uploads are handled by the separate `grape-recorder` project.

This repository (`hf-timestd`) does not use `digital_rf` and does not perform decimation.
